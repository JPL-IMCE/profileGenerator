object Versions {

  // Version history:
  // 1.3.0 - Added notion of a "Classifier" and made "Component" and "Block" a specialization of
  //          these
  // 1.2.0 - Added generation of aspects and library elements / components to conform with
  //          desired features first introduced by V&V profile (components with value properties
  //          such that these value properties can be used in parametric diagrams, etc.)
  // 1.1.0 - Added CustomizationPackage and ValidationPackage to allow for OCL validation
  //          package and MD customizations package to be exported correctly.
  // 1.0.0 - Initial release with basic classes

  // this project
  val version = "1.3.0"

}
